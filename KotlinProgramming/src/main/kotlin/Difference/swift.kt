package Difference
// kotlin | swift
// val / let
// var / var
// "$value", "${value}" / "\(value)"
// ${'specialValue'} / \specialValue
// OptionalValue ?: value / OptionalValue ?? value
// if (condition) trueValue else falseValue / condition ? trueValue else falseValue
// value.toDouble / Double(value)
// if (anyValue is String) / if let anyValue = anyValue as? String
// fun funcName(argName: DataType): returnType / func funcName(argName: DataType) -> returnType
